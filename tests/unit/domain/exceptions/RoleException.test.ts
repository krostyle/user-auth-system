describe('Role exceptions', () => {
  test('Should throw RoleException when name is empty', () => {});

  test('Should throw RoleException when name is only whitespaces', () => {});

  test('Should throw RoleException when description is empty', () => {});

  test('Should throw RoleException when description is only whitespaces', () => {});

  test('Should not throw RoleException when name and description are not empty', () => {});
});
